<%= form_with(model: user_activity_history, local: true) do |form| %>
  <% if user_activity_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_activity_history.errors.count, "error") %> prohibited this user_activity_history from being saved:</h2>

      <ul>
      <% user_activity_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :points_earned %>
    <%= form.number_field :points_earned, id: :user_activity_history_points_earned %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
